{
  "name": "@acechange/agent-wallet",
  "version": "0.1.0",
  "description": "Non-custodial, policy-scoped crypto wallet for AI agents. Keys live with the bot; AceChange never custodies. Built for Base + the AceChange Agent API.",
  "license": "MIT",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": { ".": "./dist/index.js" },
  "files": ["dist", "src", "README.md", "LICENSE"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "keywords": ["ai-agent", "wallet", "non-custodial", "base", "ethereum", "erc-4337", "x402", "mcp", "self-custody"],
  "dependencies": {
    "viem": "^2.21.0"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  },
  "engines": { "node": ">=18" }
}
